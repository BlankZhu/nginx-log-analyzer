mod analyzer;
mod config;
mod error;
mod item;
mod option;
mod result;

use clap::Clap;
use option::Option;

fn main() {
    let _cli = Option::parse();

    // let mut analyzer = Analyzer::new();

    todo!();
}
